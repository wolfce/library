<%= form_for(@books_on_loan) do |f| %>
  <% if @books_on_loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@books_on_loan.errors.count, "error") %> prohibited this books_on_loan from being saved:</h2>

      <ul>
      <% @books_on_loan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :borrower_id %><br>
    <%= f.number_field :borrower_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
